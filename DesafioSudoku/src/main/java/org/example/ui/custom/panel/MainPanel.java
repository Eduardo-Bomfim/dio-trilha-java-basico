package org.example.ui.custom.panel;

import javax.swing.*;
import java.awt.*;

public class MainPanel extends JPanel {

    public MainPanel(final Dimension dimensions) {
        this.setSize(dimensions);
        this.setPreferredSize(dimensions);
    }
}
